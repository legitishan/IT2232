const name = prompt('Enter Name: ');
const name1 = prompt('Enter Name1: ');
const d = new Date();

const result = `The names are ${name} and ${name1},the no. of ms since 1970 is ${d.getTime()} and 1+1 is ${1+1
}`;
alert(result)


